import { ArraytoStingpipePipe } from './arrayto-stingpipe.pipe';

describe('ArraytoStingpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArraytoStingpipePipe();
    expect(pipe).toBeTruthy();
  });
});
